{
  "entities": {
    "contact_submission": {
      "title": "Contact Submission",
      "description": "A message submitted via the contact form.",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "The name of the sender" },
        "email": { "type": "string", "format": "email", "description": "The email of the sender" },
        "message": { "type": "string", "description": "The message content" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp when the submission was created" }
      },
      "required": ["name", "email", "message", "createdAt"]
    }
  },
  "firestore": {
    "contact_submissions": {
      "schema": "contact_submission",
      "description": "Collection of contact form submissions"
    }
  }
}
